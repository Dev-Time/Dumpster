To run you need the have created your dumpster and defined an environment variable with:

export DUMPSTER=/home/student/DUMPSTER/

and run the makefile

./dv
./rm [-r -f]
./dump


experiments conatins all of our experiments. some require some setup (creating specific files). and most are commented out of the run.
